# Multi-Model Ollama Chat (Streamlit)

A simple Streamlit web app that lets multiple local Ollama models respond to the same chat thread.

## What you need
- Python 3.10+
- Ollama running locally (default: http://localhost:11434)

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
